name: abcd

on: push

jobs:
  Prashant:
    runs-on: windows-latest
    steps:
      - name: echo a string
        run: echo "Hello World !!"
      - name: multiline sccript
        run: |
              node -v
              npm -v
      
    